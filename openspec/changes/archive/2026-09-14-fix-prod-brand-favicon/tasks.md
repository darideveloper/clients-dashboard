## 1. Confirm diagnosis in prod (do first — decides everything)

- [x] 1.1 View-source the prod admin page, copy the `<link rel="icon" href="...">` URL per affected brand (brand_1/brand_2 CDN URLs obtained)
- [x] 1.2 Run `curl -sI "<favicon-href>"` — record status: 403/404 confirms missing file (proceed to phase 2); 200 + `image/png` means the file exists but content is wrong (**halt: do not proceed to phase 2** — record the bytes/pixels from 1.3 and reassess the generation path instead). RESULT: 200 + image/png on both brands → test-pollution path confirmed, missing-file path ruled out
- [x] 1.3 If 200: download and inspect pixels (`PIL.Image.open(...).getcolors()`), record exact RGB vs the brand logo. RESULT: solid (255,0,0), md5 b0557c08… == test-generated bytes (triple match)
- [x] 1.4 List S3 keys under `<PUBLIC_MEDIA_LOCATION>/brands/brand_<pk>/` — confirm `favicon.png` absent (or present-but-red). RESULT: keys present (200s); content match proved pollution, listing unnecessary

## 2. Existence guard in `site_favicon`

- [x] 2.1 In `utils/callbacks.py`, check `storage.exists(brand._favicon_storage_path())` (via the brand's logo storage) before returning `brand.favicon_url`; fall back to `static("favicon.png")` when absent
- [x] 2.2 Add tests in `core/tests.py`: brand with logo but deleted/missing `favicon.png` → `site_favicon` returns static fallback; brand with generated favicon → returns brand URL (existing test covers)
- [x] 2.3 Run full test suite (`python manage.py test`) — all green (note: 2 failures in `utils/test_excel_export.py` are pre-existing on the clean tree, verified unrelated via `git stash`)

## 3. Self-healing deploys

- [x] 3.1 Append `python manage.py backfill_brand_favicons` after `base_loaddata` in `start.sh`, with a `# ponytail:` ceiling note (full regen per deploy is fine while brand count is small; `--missing-only` if startup cost ever matters)
- [x] 3.2 Verify the command exits zero on per-brand failure (it catches exceptions per brand) so `set -e` can never block gunicorn — run `python manage.py backfill_brand_favicons; echo $?` locally (expect `0`), including against a brand with a corrupt/missing logo file

## 4. Repair and verify prod

- [x] 4.1 Deploy (or run `backfill_brand_favicons` manually in prod for immediate relief). DONE: backfill regenerated real per-brand icons
- [x] 4.2 Per brand: `curl -sI` the tab href → expect `200` + `image/png`; download and confirm 32×32 PNG matching the logo. DONE per user confirmation
- [x] 4.3 Confirm the tab icon visually per brand, allowing for cache lag: S3 `CacheControl: max-age=86400` + sticky browser favicon cache may delay visibility up to ~24h — hard-refresh before declaring failure. DONE: confirmed working only after CDN edge-cache purge — purge was required, not just browser refresh
- [x] 4.4 Re-run the missing-file scenario against prod behavior: a brand with a logo and no favicon must serve the static fallback, never a dead URL. COVERED by unit test 2.2 (prod never hit the missing-file path; files existed)
